[core]
	editor = nvim
	excludesfile = {{ git.gitignore.path }}

[init]
	defaultBranch = main

[user]
	name = {{ user.realname }}
	email = {{ git.email }}

[alias]
	hist = log --oneline --graph --decorate
	tree = !git hist --all
