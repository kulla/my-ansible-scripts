[core]
	editor = vim
	excludesfile = {{ git_global_ignore_file }}

[user]
	name = {{ user.realname }}
	email = {{ git.email }}

[alias]
	hist = log --oneline --graph --decorate
	tree = !git hist --all
